<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#2B2D31"
    tools:context=".fragments.RobotListScreenFragment">

    <!-- Container for the round buttons with labels under navbar -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:orientation="horizontal"
        android:layout_marginTop="20dp">

        <!-- Calendar button for setting reminder with text -->
        <LinearLayout
            android:layout_width="166dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical">

            <!-- Circular ImageView for calendar icon -->
            <ImageView
                android:id="@+id/calendarButton"
                android:layout_width="150dp"
                android:layout_height="150dp"
                android:background="@drawable/circular_background"
                android:contentDescription="Set Reminder"
                android:padding="0dp"
                android:scaleType="centerInside"
                android:src="@drawable/calender" />

            <!-- Text label for calendar button -->
            <TextView
                android:id="@+id/calendarText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Set Reminder"
                android:textAlignment="center"
                android:textColor="@color/white" />
        </LinearLayout>

        <!-- Help line button for help with text -->
        <LinearLayout
            android:layout_width="158dp"
            android:layout_height="match_parent"
            android:layout_marginStart="16dp"
            android:gravity="center"
            android:orientation="vertical">

            <!-- Circular ImageView for help icon -->
            <ImageView
                android:id="@+id/helpLineButton"
                android:layout_width="150dp"
                android:layout_height="150dp"
                android:background="@drawable/circular_background"
                android:contentDescription="Help Line Contacts"
                android:padding="24dp"
                android:scaleType="centerInside"
                android:src="@drawable/helpline" />

            <!-- Text label for help button -->
            <TextView
                android:id="@+id/helpLineText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Help Line"
                android:textAlignment="center"
                android:textColor="@color/white" />
        </LinearLayout>

    </LinearLayout>

    <!-- Button to move to chat screen -->
    <Button
        android:id="@+id/moveChatbtn"
        android:layout_width="match_parent"
        android:layout_height="66dp"
        android:layout_gravity="bottom|center_horizontal"
        android:text="Move to Chat Screen"
        android:textSize="24sp"
        android:layout_marginBottom="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginLeft="20dp"/>

</FrameLayout>
